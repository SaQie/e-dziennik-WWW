export interface CreateSchoolRequest{
    name: string
    address: string
    postalCode: string
    city: string
    nip: string
    regon: string
    phoneNumber: string
    schoolLevelId: string
}